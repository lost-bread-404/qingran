# 清然

Rosie 的语音聊天工具：按住说话 / 电话免提，Eve 朗读，长期记忆，可编辑 system prompt。

这是从 Grok 应用构建器导出的源码（含语气词识别改回「只信语音转写」的版本）。

## 本地运行

```bash
npm install
npm run dev
```

默认在 `0.0.0.0:8080`。

## 说明

- 聊天记录和记忆存在数据库里，跟发布用的域名绑定。换一个 grok.me 链接会是空库，不会自动带上旧站的 prompt / 记忆。
- 不要提交 `.env` 或 API 密钥。
