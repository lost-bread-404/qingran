# 清然：你要做的全部步骤

我这边能写进仓库的都已经写好（网页、备份导入导出、iPhone 原生壳）。  
**你还什么都没做。** 按下面顺序一次做完。做完之后：GitHub 一推代码，你的域名自动更新；iPhone 只装一次。

不要跳步。卡在哪一步，把屏幕上的原话发我。

---

## 做完之后会长成什么样

- 你自己的网址（例如 `https://qingran.你的域名`）打开就是清然
- 记忆在你自己的数据库里，换域名也可以导来导去
- GitHub 上改代码 → Vercel 自动发布 → 手机打开就是新版
- iPhone 主屏幕有「清然」App，设置里有麦克风权限，切软件也能继续通话

---

## 你要准备的

| 要什么 | 干什么 | 大概花费 |
|---|---|---|
| 任意电脑（能上网就行） | 买域名、注册 Neon / Vercel / xAI | — |
| **一台 Mac** | 把 App 装到 iPhone | — |
| iPhone（iOS 16+） | 日常用清然 | — |
| 自己的域名 | 长期网址，不再用 grok.me | 大约 $10–15 / 年 |
| Neon 账号 | 记忆和聊天的数据库 | 免费档够用 |
| Vercel 账号 | 网站托管、GitHub 自动发布 | 免费档够用 |
| xAI API 密钥 | 对话和朗读 | 用你 Grok 账号在 console.x.ai 开 |
| Apple 开发者（个人） | 签名 iPhone App | **$99 / 年** |
| 数据线 | 第一次把 App 装进手机 | — |

GitHub 仓库已经有了：https://github.com/lost-bread-404/qingran  
分支用 **`ios-microphone`**（我们一直在这支上改）。

---

# 第一部分：网站（GitHub 一推就更新）

## 1. 买一个域名

在 [Cloudflare Registrar](https://www.cloudflare.com/products/registrar/)、[Porkbun](https://porkbun.com/) 或 Namecheap 买一个你愿意长期用的名字。

建议短、好记、你不会换的。例如 `qingran.xxx` 或你想好的名字。

买完先不要乱改 DNS，后面 Vercel 会告诉你填什么。

把买到的完整域名记下来，后面到处都用它。

## 2. 开数据库（Neon）

1. 打开 https://console.neon.tech 用 GitHub 或邮箱注册。
2. **New Project**，名字填 `qingran`，区域选离你近的（美国用 `AWS US East`）。
3. 创建后进入项目，找到 **Connection string**。
4. 选 **Pooled connection**（有 `*-pooler` 字样的那条）。
5. 复制整串，以 `postgresql://` 开头。这就是 `DATABASE_URL`。
6. **不要**发到聊天里，**不要**提交到 GitHub。只粘到下一步的 Vercel 里。

## 3. 开 xAI 密钥（对话和朗读）

Grok 网页构建器里的密钥不会跟到你自己的网站。要单独开一把。

1. 打开 https://console.x.ai 用你的 xAI / Grok 账号登录。
2. 建一个 API Key，名字填 `qingran`。
3. 复制密钥（只显示一次）。这就是 `XAI_API_KEY`。
4. 同样：不发聊天、不进 GitHub，只进 Vercel。

## 4. 用 Vercel 接上 GitHub（这就是自动发布）

1. 打开 https://vercel.com 用 **GitHub 账号**登录（必须是有 `lost-bread-404/qingran` 权限的那个）。
2. **Add New… → Project** → Import `lost-bread-404/qingran`。
3. 设置：
   - **Framework Preset**：Vite（若没有就 Other）
   - **Root Directory**：`.`（仓库根目录）
   - **Build Command**：`npm run build`
   - **Node.js Version**：22.x（在 Project Settings → General）
   - **Production Branch**：`ios-microphone`
4. **Environment Variables** 加两条，Production / Preview / Development 都勾上：

   | Name | Value |
   |---|---|
   | `DATABASE_URL` | 第 2 步那串 Postgres 连接 |
   | `XAI_API_KEY` | 第 3 步那把密钥 |

5. 点 **Deploy**。第一次可能要一两分钟。
6. 成功后 Vercel 会给一个 `xxx.vercel.app` 试用链接。用电脑打开，应能看到清然。这时记忆是空的，正常。

以后：只要往 `ios-microphone` 推代码，Vercel 就会再发一版。不用再点 Grok 的发布。

## 5. 把你的域名绑到 Vercel

1. Vercel 项目 → **Settings → Domains** → 填你买的域名 → Add。
2. 按页面提示去域名服务商改 DNS（常见是）：
   - 根域名 `@`：A 记录，指到 Vercel 给的 IP  
   - `www`：CNAME，指到 `cname.vercel-dns.com`（以页面显示为准）
3. 等几分钟到几小时生效。
4. 用手机 Safari 打开 `https://你的域名`，确认是清然，而且是 **https**。

**iPhone 壳和以后分享，都只用这个自己的域名。不要用 grok.me，也不要长期用 vercel.app。**

## 6. 把旧清然的记忆搬过来（如果旧站还能打开）

仓库里已经有「导出 / 导入备份」。

1. 用 Safari 打开 **现在还能用的那个旧 grok.me 清然**。
2. 进设置 → **备份** → **导出备份**。文件会下载到手机/电脑。放到 iCloud 或文件 App，不要传到 GitHub。
3. 再打开 **你自己的新域名**。
4. 设置 → 备份 → **导入备份**，选刚才那个文件。
5. 看 prompt、记忆、聊天是不是都回来了。

旧站已经打不开：这一步做不了，新站会是空的人设，只能把 prompt 重新贴进设置。

---

# 第二部分：iPhone 原生壳（只装一次）

壳已经在仓库 `ios/` 里。它打开的是你自己的网站，所以网站更新了，App 不用重装。

## 7. 加入 Apple 开发者计划（$99 / 年）

1. 打开 https://developer.apple.com/programs/enroll/
2. 用你平时的 Apple ID，选 **Individual**。
3. 付钱、同意协议。可能要验证身份（几小时到两天）。
4. 收到 Welcome 邮件再做第 8 步。

## 8. Mac 上装 Xcode 并登录

1. Mac App Store 安装 **Xcode**（很大）。
2. 打开一次，同意条款。
3. **Xcode → Settings → Accounts → + → Apple ID**，登付钱那个账号。
4. 右边要出现你的 **Team**。

## 9. 拿到工程

会 Git：

```
git clone https://github.com/lost-bread-404/qingran.git
cd qingran
git checkout ios-microphone
```

不会 Git：打开 https://github.com/lost-bread-404/qingran → 分支选 `ios-microphone` → Code → Download ZIP → 解压。

## 10. 签名

1. 双击 `ios/Qingran/Qingran.xcodeproj`（用 Xcode 打开）。
2. 左边点蓝色 **Qingran** → TARGETS 里的 Qingran → **Signing & Capabilities**。
3. 勾 **Automatically manage signing**。
4. **Team** 选你的付费个人团队。
5. Bundle Identifier 保持 `app.qingran.ios`；若提示被占用，改成 `app.qingran.你的名字`。
6. 没有红字再继续。

## 11. 装到 iPhone

1. 数据线连上 Mac，信任这台电脑。
2. iPhone：**设置 → 隐私与安全性 → 开发者模式** 打开（没有就先在 Xcode 顶部选中你的 iPhone）。
3. Xcode 顶部设备改成你的 iPhone，点左上角 ▶ Run。
4. 若提示不受信任：**设置 → 通用 → VPN 与设备管理** → 信任你的 Apple ID。
5. 主屏幕出现「清然」。

## 12. 贴你自己的域名、开麦克风

1. 打开的必须是主屏幕上这颗 **清然 App**，不是 Safari，也不是以前「添加到主屏幕」的图标。
2. 第一屏贴：`https://你的域名`（第 5 步那个）。
3. 点打开清然 → 点电话 → 麦克风选 **允许**。
4. 以后开关在：**设置 → 清然 → 麦克风**。
5. 切到别的软件可以继续说。从多任务**划掉**清然 = 挂断。

地址贴错了：**设置 → 清然 → 网址** 改掉，再把 App 划掉重开。

---

# 第三部分：做完以后怎么用

## 日常改功能

1. 我在 GitHub `ios-microphone` 上改代码并 push（或你自己改）。
2. Vercel 自动部署（项目里能看到 Deployment）。
3. 手机上没在打电话时，把清然切到前台，就是新版。

不用重装 App，不用再点 Grok 发布。

## 只有这些才要重装 iPhone

改了 `ios/` 里面的原生壳（麦克风、CallKit）。那种情况再用 Xcode 点一次 Run。

## 明年

- 域名续费
- Apple 开发者续 $99，再用 Xcode 连手机点一次 Run
- Neon / Vercel 免费档继续即可
- xAI 密钥按你的用量

## 不要做

- 不要把 `DATABASE_URL`、`XAI_API_KEY`、备份 JSON 提交到 GitHub
- 不要在新的 Grok chat 里再发布一个 grok.me 来「代替」这个站
- 不要把 App 提交到 App Store
- 不要发 TestFlight 公开链接

---

# 常见卡住的地方

**Vercel 构建失败**  
看 Deploy 日志。把红字原文发我。常见是忘了填 `DATABASE_URL`，或 Node 不是 22。

**网站能开但不能说话 / 不能朗读**  
`XAI_API_KEY` 没填，或填错环境（要 Production）。

**自己的域名打不开**  
DNS 还没生效，或多等一会儿；确认是 https。

**Xcode Team 是空的**  
开发者计划还没通过，或登错 Apple ID。

**导入备份没反应**  
必须在**新域名的网站**上导入；导出必须来自设置里的「导出备份」，不是随便一个 json。
