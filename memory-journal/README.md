# 记忆工作底稿

清然每次整理记忆时，会把过程追加进这些文档（带时间戳，只追加不覆盖）：

- `chat.md` 聊天原文
- `l1.md` 已结束的事件
- `l2.md` 按周期收束后的叙述
- `l3.md` 规律快照（每次 C 重写都留一版）
- `portrait.md` 活画像历史（每次覆盖写之前都留一版）
- `open.md` 未闭合事件草稿的变化
- `process.md` A / B / C 判定和模型原文

线上同一份也进数据库，盘只读时文档写不进去也不挡说话。

`sample/` 里是一次四天样例，用来看 L1 写得够不够高。
