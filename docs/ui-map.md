# 设置在哪

从设置首页进去，任何一项最多再点两次。平时只看，不用调：模型自己学。

| 想看 / 想改的 | 页面 |
|---|---|
| 身份、人设、亲密设定、模式（名字、什么时候用、prompt、temperature、亲密设定、记不记动作）、故事线 | 清然是谁 |
| 心里、眼前这一件、打算、当前模式、今天、最近几天 | 他的心 |
| 他记得的、版本（带每晚改了什么）、现在把今天整理进去 | 他的心 |
| 主动消息开关、测试通知、现在让他想起你、记录 | 主动消息 |
| 语速、静音、听力低 / 中 / 高 | 声音和听力 |
| 导出 / 导入（初始化）、清空聊天、退出 | 数据 |
| 运行心思和记忆整理、他心里 / 他记得的开关、上下文长度、人设放在哪；指令（每轮回复、内心、整理记忆、人设之后、月报）和每条的模型 | 高级 → 指令 |
| 调用记录、手改、状态（已标条数、模型、库占用）、改动记录、重放对比 | 高级 → 记录 |
| 费用（只记账） | 高级 → 费用 |
| 听力具体滑块、keyterm、同音词、标注 | 高级 → 听力参数 |

日记页只有每天的时间线和月报。

主屏幕没有调试行。
